name: Diagnóstico temporário do lint editorial

on:
  push:
    branches:
      - agent/restore-editorial-hero

permissions:
  contents: read

jobs:
  lint-diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Instalar dependências
        run: npm ci --no-audit --no-fund

      - name: Capturar lint
        run: npm run lint > lint.log 2>&1 || true

      - name: Publicar relatório
        uses: actions/upload-artifact@v4
        with:
          name: lint-editorial-report
          path: lint.log
          if-no-files-found: error
          retention-days: 1
