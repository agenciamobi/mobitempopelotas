name: SACE validation temp

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Validação SACE concluída; arquivo temporário pode ser removido."
