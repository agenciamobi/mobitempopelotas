name: Qualidade

on:
  push:
    branches:
      - main
      - agent/**
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Build, typecheck e lint
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Instalar dependências
        run: npm ci --no-audit --no-fund

      - name: Formatar lote web push
        if: github.event_name == 'push' && github.ref == 'refs/heads/agent/web-push-notifications'
        run: |
          npx prettier --write \
            public/sw.js \
            src/components/pwa/PushNotificationsManager.tsx \
            src/lib/push/push-storage.server.ts \
            src/lib/push/web-push.server.ts \
            src/routes/api/cron/push-daily.ts \
            src/routes/api/push/broadcast.ts
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/sw.js src/components/pwa/PushNotificationsManager.tsx src/lib/push/push-storage.server.ts src/lib/push/web-push.server.ts src/routes/api/cron/push-daily.ts src/routes/api/push/broadcast.ts
          if ! git diff --cached --quiet; then
            git commit -m "style: formata implementação web push"
            git push origin HEAD:${GITHUB_REF_NAME}
          fi

      - name: Build de produção
        run: npm run build

      - name: TypeScript
        run: npm run typecheck

      - name: ESLint
        run: npm run lint
