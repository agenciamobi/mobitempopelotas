name: Qualidade

on:
  push:
    branches:
      - main
      - agent/**
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Build, typecheck e lint
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Instalar dependências
        run: npm ci --no-audit --no-fund

      - name: Build de produção
        run: npm run build

      - name: TypeScript
        run: npm run typecheck

      - name: ESLint
        shell: bash
        run: |
          mkdir -p artifacts/lint
          set +e
          npm run lint 2>&1 | tee artifacts/lint/output.log
          status=${PIPESTATUS[0]}
          exit "$status"

      - name: Publicar diagnóstico ESLint
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lint-${{ github.run_number }}
          path: artifacts/lint
          if-no-files-found: warn
          retention-days: 2
